# 本機 wrangler dev 用的機密值範本
# 複製為 .dev.vars（已列入 .gitignore），填入實際值後再跑 `pnpm worker:dev`。
# 正式環境請改用：npx wrangler secret put <名稱>
LINE_CHANNEL_ACCESS_TOKEN=your-line-channel-access-token
LINE_USER_ID=your-line-user-id

# 本機放行擁有者端點（/owner/api/*）。
# 正式環境的閘門是 Cloudflare Access，但本機 wrangler dev 沒有 Access，
# 請求不會帶 Cf-Access-Jwt-Assertion header，因此需要這個旗標才能在本機測試寫入功能。
#
# ⚠️ 絕對不要在正式環境設定這個變數 —— 設了等於把寫入端點完全對外開放。
ALLOW_INSECURE_OWNER=true
